{
  "name": "banana-hunter",
  "version": "1.0.0",
  "private": true,
  "description": "Banana Hunter endless runner - native Android wrapper via Capacitor",
  "scripts": {
    "sync:web": "powershell -ExecutionPolicy Bypass -File ./sync-www.ps1",
    "cap:sync": "cap sync android",
    "apk:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
